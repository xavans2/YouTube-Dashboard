# Xavis Analytics Kanban Done Archive

## Existing Baseline

The following capabilities existed before this planning backlog was created and are treated as the current product baseline rather than active stories:

- Terminal-style dashboard and dynamic channel branding.
- `/vid-data`, `/top-vids`, `/alerts`, `/system`, and `/help` page shells.
- SQLite history with 30-day retention and carry-forward behavior.
- GitHub repository, `.env.example`, and ignored local data/secrets.

No newly planned stories have been merged yet.

